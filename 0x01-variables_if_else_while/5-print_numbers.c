#include <stdio.h>

/**
 * main - Entry point
 * created by Bayomi on 13 Dec 01:30
 * Return: Always 0 (Success)
 */

int main(void)
{

int singleDigit;

for (singleDigit = 0; singleDigit < 10; singleDigit++)

printf("%d", singleDigit);

printf("\n");

return (0);
}
