
#include "main.h"

/**
 * mul - Entry point
 * 05am 17th Dec by Bayomsss
 * @c: argument
 * int: data type
 * Return: 0 and 1 success
 */

int mul(int a, int b)
{
int prod, prd;

prod = mul(a, b);
prd = mul(a, b);

_putchar(prod);
_putchar(prd); 

return (0);
}
