#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - computes absolute value of integer
 * @n: integer in question
 * 07:37am 15th Dec by Bayomsss
 * Return: Always 0
 */

int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
