#include "main.h"
#include <ctype.h>

/**
 * _islower - check the code
 * 04:07am 15th Dec by Bayomsss
 * Return: Always 0.
 */

int _islower(int c)
{
int character;

character = 'c';

if (islower(character))
{
_putchar (1);
}
else
_putchar (0);
}
